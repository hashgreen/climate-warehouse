apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "climate-warehouse.fullname" . }}-test-connection"
  labels:
    {{- include "climate-warehouse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "climate-warehouse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
